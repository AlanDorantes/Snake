#pragma once
#include <Snake.hpp>

class Partida
{
private:
    /* data */
public:
    bool Iniciar() {}
    bool Terminar(Snake) {}
    bool Reiniciar(Snake) {}
    Partida(/* args */) {}
    ~Partida() {}
};